## New Features

- **Strata Support**: Added `SetFrameStrata` layers to support correct z-ordering and rendering layers for UI components (e.g. tooltips drawing on top of normal dialogs).

## Changes

- **Pattern Matching**: Substantially enhanced the `gmatch` Lua-to-Python pattern converter to translate character classes and escape patterns correctly.
